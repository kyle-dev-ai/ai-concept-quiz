# Domain glossary

## 어텐션!

답을 읽기 전에 AI 개념을 자기 말로 설명하고 이해 상태를 기록하는 학습 제품의 작업명이다.

## 학습자 유형

사용자가 현재 놓인 맥락이다. 10대 학생, 대학생, 대학원 준비·재학, 이직 준비, 직장인, AI 입문자로 구분한다. 난이도와 첫 설명 방식에 영향을 주지만 능력을 판정하지 않는다.

## 학습 목표

사용자가 AI를 공부하는 이유다. AI 기초, 대학원 진학, 직무 전환·이직, AI 실무 역량으로 구분하며 추천 카테고리 순서를 결정한다. 학습자 유형과 별개의 개념이다.

## 질문 덱

한 학습 세션에 중복 없이 제시되는 질문 순서다. 추천 덱, 전체 랜덤 덱, 카테고리 덱이 있다.

## 10초 구술

정답 공개 전 화면을 보지 않고 핵심 흐름을 짧게 소리 내어 설명하는 학습 행동이다. 정확히 10초를 강제하는 timer가 아니다.

## 10초 핵심 답변

질문에 직접 답하는 2~3문장의 짧은 기준 답변이다. 심화 답변보다 먼저 보이며, 단순 정의보다 이유와 동작을 포함한다.

## 답변 방식

답을 떠올려 남기는 입력 경로다. `소리 내어 말하기`는 브라우저 음성 인식으로 발화를 받아적고,
`무음`은 마이크를 켜지 않고 핵심 키워드를 적는다. 학습 화면에서 바로 바꿀 수 있으며 기본값은
소리 내어 말하기다. 판정 기준은 두 방식이 같고, 유사도(%)는 소리 내어 말한 기록에만 남는다.

## 자기평가

답을 본 뒤 사용자가 `알았다`, `애매했다`, `몰랐다` 중 하나로 남기는 회상 상태다. 자동 채점이나 객관적 능력 판정이 아니다.

## 설명력 점수

현재 corpus 전체를 기준으로 자기평가를 0–100으로 환산한 기기 내 학습 진도다. 시험 점수, 지능 점수 또는 사용자 간 순위가 아니다.

## 성장 레벨

설명력 점수 구간을 친근하게 나타내는 단계다. 씨앗 질문가, 개념 탐험가, 연결 설계자, 설명 훈련가, AI 구술가로 구성하며 경쟁 등급이 아니다.

## Candidate

AI나 사람이 작성했지만 아직 schema, golden evaluation, 사람 검수를 모두 통과하지 않은 질문 초안이다.

## Reviewed corpus

앱에서 사용자에게 제공하도록 승인된 질문 집합이다. stable ID와 dataset version을 가진다.

## Golden set

핵심 설명과 자주 생기는 오개념이 바뀌지 않았는지 candidate와 독립적으로 확인하는 versioned 평가 사례다. 개발, 회귀, 도전 suite를 분리한다.

## Profile

앱이 실행되는 환경이다. `local`, `standalone`, `prd`로 구분한다. 사용자 프로필과 혼동하지 않는다.

## 화면 모드

앱의 명암 표현 설정이다. `라이트`, `다크`, `기기 설정`으로 구분하며 신규 사용자는 라이트가 기본이다. 학습자 유형이나 학습 목표와 독립적이고 선택은 현재 channel의 기기 저장소에만 남는다.

## 학습자 프로필

닉네임, 학습자 유형, 학습 목표와 한 줄 목표를 모아 보여주는 primary navigation 화면이다. 실행 환경을 뜻하는 `Profile`과 구분한다.

## 광고 구좌

광고 provider가 붙을 수 있는 화면 위치의 stable name이다. 광고 자체가 아니며 비활성, No Fill, 실패, 미지원 상태에서는 화면 공간을 차지하지 않는다.

## Local-first

서버 계정 없이 콘텐츠와 진도를 먼저 기기에서 사용할 수 있는 제품 원칙이다. 기기 간 동기화나 영구 보존을 의미하지 않는다.
